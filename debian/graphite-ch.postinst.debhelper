# Automatically added by dh_installinit
if [ -x "/etc/init.d/graphite-ch" ] || [ -e "/etc/init/#SCRIPT#.conf" ]; then
	if [ ! -e "/etc/init/graphite-ch.conf" ]; then
		update-rc.d graphite-ch defaults >/dev/null
	fi
	invoke-rc.d graphite-ch start || exit $?
fi
# End automatically added section
